version: '3.8'

services:
  osrm:
    image: osrm/osrm-backend
    container_name: osrm_ph
    restart: unless-stopped
    volumes:
      - ./osrm-data:/data
    ports:
      - "5000:5000"
    command: osrm-routed --algorithm mld /data/philippines-latest.osrm
